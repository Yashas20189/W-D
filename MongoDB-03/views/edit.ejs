<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <h2>Edit your chat: <%= chat._id %></h2>
    <p>This chat was sent from <%= chat.from %> to <%= chat.to %></p>
    <!-- Method override below -->
    <form method="POST" action="/chats/<%= chat._id%>?_method=PUT"> 
        <textarea name="msg" rows="5" cols="50">
            <%= chat.msg %>
        </textarea>
        <button>Edit</button>
    </form>
</body>
</html>

  <!-- we primarily use POST and method override instead of GET for actions like updating or deleting data -->