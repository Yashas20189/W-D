// let n =5;

// for (let i=0; i<n ; i++){
//     console.log("hello,",i);
// }

// node app.js(name of js) -> we can print output in terminal 

console.log(process.argv);

// process.argv -> used to pass argument (values) to git bash

// module.export
const math = require("./math");
console.log(math);